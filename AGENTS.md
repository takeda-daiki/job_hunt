# AGENTS.md — 就職条件ノート

## 目的

このリポジトリでは、ユーザーからの短い自然言語の改善要求を、実際に動作するコード変更まで自律的に完了すること。

ユーザーは「○○できるようにして」「ここを見やすくして」など、実装方法を指定せずに要求することがある。
その場合も、単に実装方法を説明するのではなく、既存コードを調査し、必要なファイルを判断して実際に修正する。

原則として、ユーザーにコードを手作業で編集・コピペさせない。

例外は Supabase Dashboard など、リポジトリ外の環境でしか実行できない操作である。
その場合は、リポジトリ側で可能な変更を先に完了したうえで、ユーザーが実行すべき内容を最後に明確に提示する。


## このアプリの構成

このアプリは、GitHub Pages 等でそのまま配信できる静的 Web アプリである。

主要ファイル:

- `index.html`
  - アプリのエントリーポイント。
- `app.js`
  - アプリ本体。
  - UI生成、状態管理、計算、Supabase Auth、Database、Storageとの通信を担当する。
- `styles.css`
  - PC・スマートフォン双方のUI。
- `config.js`
  - Supabase Project URL と Publishable/Anon key。
- `service-worker.js`
  - PWAキャッシュとオフライン時のフォールバック。
- `manifest.webmanifest`
  - PWA設定。

現在は npm / bundler / framework を前提としていない。

小規模な機能追加のためだけに React、Vite、npm 等へ全面移行しないこと。
既存構成で実装できる場合は既存構成を維持する。


## 基本的な作業方針

ユーザーの要求は「実装仕様」ではなく「達成したい結果」と解釈する。

要求を受けたら原則として以下を自律的に行う。

1. 関連する既存コードを調べる。
2. 現在のデータ構造と画面遷移を把握する。
3. 最小限かつ自然な実装方法を決める。
4. 必要なファイルを直接修正する。
5. 関連機能への影響を確認する。
6. 可能な範囲で検証する。
7. 必要なら Supabase 用SQLを作成する。
8. 最後に変更内容と外部作業の有無を報告する。

「こう変更できます」と説明するだけで終了しない。
実装可能なものは実装する。

不要な確認質問は避ける。

複数の安全な実装方法がある場合は、既存設計と最も整合する方法を選択する。

ただし、次の場合は推測で破壊的変更を行わない。

- 既存データを削除する可能性がある
- 大規模なデータ移行が必要
- 認証方式を変更する
- 明確に異なる2つ以上の仕様が考えられ、選択によってユーザーデータの意味が変わる


## 変更の原則

既存機能を壊さないことを最優先する。

機能追加では、必要以上のリファクタリングを行わない。

同じ目的を小さな変更で達成できるなら、小さい変更を優先する。

既存の命名、画面デザイン、データ構造、操作方法になるべく合わせる。

UI上の文言は原則として日本語にする。

PC表示だけでなく `max-width: 820px` のスマートフォン表示も確認する。

既存のユーザーデータとの後方互換性を維持する。


## app.js の変更

`app.js` はアプリの中心なので、局所的な修正だけを見て変更しない。

機能を変更するときは必要に応じて以下を確認する。

- `state`
- データロード処理
- render / view生成
- イベントハンドラ
- submit処理
- Supabase read / insert / update / delete
- 初期データ生成
- モバイル表示

既存データが存在する状態でも動作するようにする。

ユーザー入力やDB由来の文字列をHTMLへ埋め込む場合は、既存の `esc()` 等を使用し、HTMLインジェクションを発生させない。


## Supabase

Supabase はこのアプリの外部環境である。

CodexがSupabaseへ直接変更を適用できない環境では、
「Supabaseも変更しました」と報告してはならない。

フロントエンド変更だけで完結する場合は、Supabase変更を要求しない。


### Supabase変更が必要な場合

以下のような変更ではSQLが必要になる可能性がある。

- テーブル追加
- カラム追加
- インデックス追加
- unique 制約追加
- foreign key追加
- RLS変更
- Storage policy変更
- 初期データ追加

必要な場合は、まずアプリ側の必要な実装を行う。

そのうえでユーザーが Supabase SQL Editor にそのまま貼り付けられる
**完全なSQL** を最終回答に提示する。

断片的なSQLではなく、可能な限り1つの実行可能なSQLブロックにまとめる。

SQLだけで実行可能な操作について、ユーザーにGUIでの複雑な操作を要求しない。


## Supabase migration

新しいDB変更を行う場合は、可能なら

`supabase/migrations/`

を作成し、新しい変更分のSQLもリポジトリに保存する。

例:

`supabase/migrations/20260925_add_company_priority.sql`

ただし、現在のSupabase全体のスキーマがリポジトリに存在しない場合、
推測で「初期スキーマ」を作成してはならない。

今回必要になった差分だけをmigrationとして保存する。

migrationに保存したSQLと、ユーザーへ提示するコピペ用SQLは同じ内容にする。


## Supabaseの安全性

既存データを守ること。

原則として以下を避ける。

- `DROP TABLE`
- `TRUNCATE`
- 全データDELETE
- カラム削除
- 既存カラム型の破壊的変更
- テーブルの作り直し

これらが本当に必要な場合は勝手に実行方法を確定せず、理由と影響を明示する。

通常は additive migration を優先する。

例:

- 新しいテーブルを追加
- 新しいnullable columnを追加
- indexを追加
- policyを追加


## RLS

ブラウザから Supabase に直接アクセスするアプリなので、RLSを重要なセキュリティ境界として扱う。

フロントエンド側で

`user_id === current user`

になるよう絞り込んでいるだけではセキュリティ対策として不十分である。

ユーザー固有データを保存する新しいテーブルには原則として:

- `user_id`
- RLS有効化
- `auth.uid() = user_id`

に基づく適切な SELECT / INSERT / UPDATE / DELETE policy

を用意する。

既存のRLSを理由なく無効化しない。


## Supabase Storage

Storageを追加・変更する場合も、認証ユーザーが自分のファイルだけを扱える構造にする。

ユーザーIDを含むパスを基本とし、Storage policyでもアクセス範囲を制限する。

公開bucketへ変更する必要がない限り、勝手に公開しない。


## Secrets

`config.js` にはクライアントから利用する Supabase Publishable / Anon key のみを置く。

以下は絶対にフロントエンドやGitHubへ追加しない。

- Supabase service_role key
- Database password
- private key
- personal access token
- その他サーバー専用secret

secretを使わないと実現できない機能の場合は、ブラウザ側へsecretを埋め込まず、別の安全な方式が必要であることを報告する。


## PWA / service worker

現在のキャッシュ方式を理解せずに変更しない。

`service-worker.js` の `SHELL` に含まれるファイルを追加・削除・名称変更した場合は `SHELL` も更新する。

キャッシュ構造やオフライン動作を変更した場合は `CACHE` のバージョン更新も検討する。

通常のUI修正だけを理由にキャッシュ方式そのものを変更しない。


## 削除処理

既存テーブルに `deleted_at` を利用した soft delete の仕組みがある場合、新機能でも原則としてその方式を優先する。

ユーザーが明確に完全削除を求めていない限り、既存データを不用意に物理削除しない。


## UI

既存のデザイン言語を維持する。

可能な限り既存の以下を再利用する。

- `.card`
- `.btn`
- `.badge`
- `.tabs`
- `.modal`
- `.form-grid`
- CSS変数

新機能のためだけに別のUIライブラリを導入しない。

PCとスマートフォンの両方で操作できるようにする。


## 検証

変更後は可能な範囲で必ず検証する。

最低限確認すること:

- JavaScriptの構文エラーがない
- HTML/CSSの明らかな破損がない
- 変更箇所から参照される関数・変数が存在する
- Supabaseのテーブル名・カラム名が一貫している
- 保存処理と読み込み処理が対応している
- 既存データがない場合にも動く
- 既存データがある場合にも動く
- モバイルレイアウトを壊していない

実行できない検証を「成功した」と報告しない。


## 完了時の回答

作業後の回答は簡潔にする。

基本形式:

### 変更したこと
- 実際に変更した内容

### 確認したこと
- 実行した検証
- 残っている制約があればその内容

### 外部作業
外部作業が不要なら:

`Supabase側の作業はありません。`

必要なら:

`Supabase側で次の操作が必要です。`

と書き、続けて実行順に提示する。


## Supabase SQLを提示するとき

以下の形式にする。

1. Supabase Dashboard を開く
2. SQL Editor を開く
3. 下記SQLをそのまま貼り付けて実行する
4. 成功後にアプリを再読み込みする

```sql
-- 完全なコピペ用SQL
```

必要なら、

- このSQLが何を変更するか
- 既存データへの影響
- 実行後に何ができるようになるか

を短く説明する。


## 最重要ルール

ユーザーが短い改善要求だけを送った場合でも、
実装仕様をユーザーに考えさせない。

既存コードから適切な実装を判断し、

**調査 → 実装 → 関連修正 → 検証 → 必要な外部作業の提示**

までを1つのタスクとして扱う。

ユーザーに任せるのは、
Codexから直接操作できない外部環境での最小限の操作だけとする。


## 利用量を抑えるための調査方針

小規模な変更では、まず要求に直接関係するファイル・関数だけを調査する。

単純なUI修正や局所的な機能変更のために、
毎回リポジトリ全体や app.js 全体を読み直さない。

必要性が判明した場合にのみ調査範囲を広げる。

小規模な変更では subagent を原則使用しない。
並列調査は、複数領域にまたがる複雑な変更で明確な利点がある場合だけ使用する。

Web検索は、外部仕様や最新情報の確認が必要な場合だけ行う。

単純な変更では大規模なリファクタリングや全面監査を行わず、
要求を満たす最小限の変更と必要な回帰確認に留める。